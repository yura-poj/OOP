package ru.nsu.pozhidaev;

/**
 * subclass of player, does not have some special behavior yet.
 */
public class User extends Player {
    /**
     * init.
     *
     * @param deck class deck.
     */
    public User(Deck deck) {
        super(deck);
    }
}
